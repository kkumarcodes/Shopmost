const path = require('path')

module.exports = {
  siteMetadata: {
    title: `shopmost`,
    description: ``,
    author: `Kyle`,
  },
  plugins: [
    `gatsby-plugin-image`,
    `gatsby-plugin-react-helmet`,
    `gatsby-transformer-sharp`,
    `gatsby-plugin-sharp`,
  ],
}
